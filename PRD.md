# 今日热搜 PRD

## 产品概述
一个多平台热搜聚合网站，首页以卡片网格展示微博、知乎、B 站等平台热榜。
访客打开链接即可浏览，无需登录。

## 目标用户
- 想快速扫一眼各平台热点的上班族、学生
- 对 Vibe Coding / 全栈开发感兴趣的学习者

## 核心功能

### MVP（必须做）
1. **首页卡片网格**
   - ≥3 张卡片：微博热搜、知乎热榜、B 站热搜
   - 每张卡片显示：平台名、榜单名、Top 10～20 列表
   - 每条热搜包含：排名、标题、跳转链接（热度 heat 可选，有则展示）

2. **数据接口（自建后端）**
   - `GET /api/hot/:source`（source = weibo | zhihu | bilibili）
   - `GET /api/hot`（一次返回全部平台，推荐）
   - 统一 JSON 格式（见技术设计文档）

3. **缓存机制（详细）**
   - 每个平台独立缓存，存储在后端内存中
   - 默认 TTL = 600 秒（10 分钟），可通过环境变量 `CACHE_TTL` 调整
   - 缓存命中时直接返回数据，不重复请求上游接口
   - 支持开发环境参数 `?refresh=1` 强制跳过缓存
   - 前端展示「更新于 X 分钟前」，该时间来自后端 `updatedAt` 字段

4. **异常处理与降级**
   - **单平台失败**：后端返回 `{ error: true, message: "暂时无法获取", items: [] }`，前端该卡片显示友好错误提示（例如“加载失败，点击重试”）
   - **整后端挂掉**：前端应显示全局错误提示，但不白屏（可用 fallback UI）
   - **上游接口超时/限流**：后端应捕获异常，返回错误态，不抛出 unhandled rejection
   - **空数据**：若某平台返回 0 条数据，卡片显示「暂无热搜」

5. **部署与合规**
   - 代码托管至 GitHub
   - 前端部署至 Vercel，后端部署至 Railway（或自选）
   - 公网 HTTPS 可访问
   - **页脚必须包含**：
     - “本站为个人学习项目，数据来源于各平台公开接口，非官方”
     - 数据更新频率（与缓存 TTL 一致）
     - 若需删除数据或侵权联系可留邮箱占位（可选）

### 后续可以做（不进入 MVP）
- 更多平台（抖音、百度、小红书）
- 分类 Tab（科技/娱乐/社会）
- 站内搜索（搜索已缓存的标题）
- 历史热榜曲线、暗色模式

## 界面设计要求
- 参考今日头条的信息密度：卡片清晰、分区明确
- 桌面端 3 列网格，平板 2 列，手机 1 列（响应式）
- 排名 1~3 用特殊颜色（金、银、铜或红橙渐变）
- 卡片底部显示“更新于 X 分钟前”及重试按钮（错误态时）
- 页脚完整合规文案

## 技术栈
- 前端：React + TypeScript + Vite + CSS（CSS Modules 或普通 CSS）
- 后端：Node.js + Express
- 部署：示例 Vercel（前端） + Railway（后端），不强制统一

## 明确不做的功能（本期）
- 用户登录 / 账号系统
- 热搜关键词搜索
- 平台分类 Tab
- 历史数据存储